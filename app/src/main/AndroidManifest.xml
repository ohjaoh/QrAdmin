<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QrAdmin"
        tools:targetApi="31">
        <activity
            android:name=".SettingActivity"
            android:exported="true"
            android:label="SettingActivity" />
        <activity
            android:name=".AdminActivity"
            android:exported="true"
            android:label="AdminActivity" />
        <activity
            android:name=".AdminScheduleActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:label="AdminScheduleActivity" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="LoginActivity" />
        <activity
            android:name=".AdminFormActivity"
            android:exported="true"
            android:label="AdminFormActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pr_edit">
        </activity>
        <activity
            android:name=".CreateQR"
            android:exported="false" />
    </application>

</manifest>